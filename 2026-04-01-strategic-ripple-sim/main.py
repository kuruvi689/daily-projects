import json\n\nclass Consequence:\n    def __init__(self, description: str, order: int, impact_score: float = 0.0, probability: float = 1.0, downstream_consequences=None):\n        self.description = description\n        self.order = order\n        self.impact_score = impact_score # e.g., -10 (very negative) to 10 (very positive)\n        self.probability = probability # 0.0 to 1.0\n        self.downstream_consequences = downstream_consequences if downstream_consequences is not None else []\n\n    def add_downstream(self, consequence):\n        if consequence.order != self.order + 1:\n            raise ValueError(f"Downstream consequence order must be {self.order + 1}, but got {consequence.order}")\n        self.downstream_consequences.append(consequence)\n\n    def to_dict(self):\n        return {\n            "description": self.description,\n            "order": self.order,\n            "impact_score": self.impact_score,\n            "probability": self.probability,\n            "downstream_consequences": [c.to_dict() for c in self.downstream_consequences]\n        }\n\n    @classmethod\n    def from_dict(cls, data):\n        consequence = cls(\n            description=data["description"],\n            order=data["order"],\n            impact_score=data.get("impact_score", 0.0),\n            probability=data.get("probability", 1.0)\n        )\n        for ds_data in data.get("downstream_consequences", []):\n            consequence.add_downstream(Consequence.from_dict(ds_data))\n        return consequence\n\nclass Decision:\n    def __init__(self, name: str, description: str, initial_consequences=None):\n        self.name = name\n        self.description = description\n        self.initial_consequences = initial_consequences if initial_consequences is not None else []\n\n    def add_initial_consequence(self, consequence: Consequence):\n        if consequence.order != 1:\n            raise ValueError("Initial consequences must be of order 1.")\n        self.initial_consequences.append(consequence)\n\n    def to_dict(self):\n        return {\n            "name": self.name,\n            "description": self.description,\n            "initial_consequences": [c.to_dict() for c in self.initial_consequences]\n        }\n\n    @classmethod\n    def from_dict(cls, data):\n        decision = cls(\n            name=data["name"],\n            description=data["description"]\n        )\n        for ic_data in data.get("initial_consequences", []):\n            decision.add_initial_consequence(Consequence.from_dict(ic_data))\n        return decision\n\nclass StrategicRippleSim:\n    def __init__(self, decision: Decision):\n        self.decision = decision\n        self.all_consequence_paths = [] # Stores lists of (consequence_object, cumulative_path_probability) tuples representing a path\n\n    def _traverse_consequences(self, current_consequences_list, current_path_items=None, current_path_probability=1.0):\n        if current_path_items is None:\n            current_path_items = []\n\n        for consequence in current_consequences_list:\n            path_prob_up_to_here = current_path_probability * consequence.probability\n            new_path_item = (consequence, path_prob_up_to_here)\n            new_path_items_list = current_path_items + [new_path_item]\n\n            self.all_consequence_paths.append(new_path_items_list)\n\n            if consequence.downstream_consequences:\n                self._traverse_consequences(\n                    consequence.downstream_consequences,\n                    new_path_items_list, # Pass the path items up to the current consequence\n                    path_prob_up_to_here # Pass the cumulative probability up to the current consequence\n                )\n\n    def simulate(self, max_order: int = 3):\n        """\n        Simulates the ripple effects of the decision up to a maximum order.\n        """\n        self.all_consequence_paths = []\n        for initial_consequence in self.decision.initial_consequences:\n            if initial_consequence.order <= max_order:\n                self._traverse_consequences(\n                    [initial_consequence],\n                    [],\n                    1.0\n                )\n        return self._summarize_results(max_order)\n\n    def _summarize_results(self, max_order: int):\n        summary = {\n            "decision": self.decision.name,\n            "total_simulated_paths": len(self.all_consequence_paths),\n            "overall_weighted_impact": 0.0,\n            "consequences_summary_by_order": {}\n        }\n\n        aggregated_consequences = {} # key: (description, order) -> value: {description, order, base_impact_score, cumulative_weighted_impact, sum_of_path_probabilities_to_this_consequence, number_of_paths_leading_here}\n\n        for path in self.all_consequence_paths:\n            for consequence, prob_up_to_this_consequence in path:\n                if consequence.order > max_order:\n                    continue\n\n                weighted_impact = consequence.impact_score * prob_up_to_this_consequence\n                summary["overall_weighted_impact"] += weighted_impact\n\n                key = (consequence.description, consequence.order)\n                if key not in aggregated_consequences:\n                    aggregated_consequences[key] = {\n                        "description": consequence.description,\n                        "order": consequence.order,\n                        "base_impact_score": consequence.impact_score,\n                        "cumulative_weighted_impact": 0.0,\n                        "sum_of_path_probabilities_to_this_consequence": 0.0,\n                        "number_of_paths_leading_here": 0\n                    }\n                aggregated_consequences[key]["cumulative_weighted_impact"] += weighted_impact\n                aggregated_consequences[key]["sum_of_path_probabilities_to_this_consequence"] += prob_up_to_this_consequence\n                aggregated_consequences[key]["number_of_paths_leading_here"] += 1\n\n        for i in range(1, max_order + 1):\n            summary["consequences_summary_by_order"][f"order_{i}"] = []\n\n        for key, agg_info in aggregated_consequences.items():\n            order_key = f"order_{agg_info['order']}"\n            if order_key in summary["consequences_summary_by_order"]: # Using 'in' for robustness if a specific order has no consequences\n                summary["consequences_summary_by_order"][order_key].append(agg_info)\n\n        for order_key in summary["consequences_summary_by_order"]:\n            summary["consequences_summary_by_order"][order_key].sort(key=lambda x: x["cumulative_weighted_impact"], reverse=True)\n\n        return summary\n\ndef run_simulation(decision_data: dict, max_order: int = 3) -> dict:\n    """\n    SaaS-ready function to run a simulation from a dictionary input.\n    """\n    decision = Decision.from_dict(decision_data)\n    simulator = StrategicRippleSim(decision)\n    results = simulator.simulate(max_order)\n    return results\n\nif __name__ == "__main__":\n    # Example Usage:\n    # Decision: "Launch a new AI-powered educational platform"\n\n    # First-order consequences\n    c1_1 = Consequence("Increased student engagement", 1, impact_score=8, probability=0.8)\n    c1_2 = Consequence("High initial development cost", 1, impact_score=-7, probability=0.95)\n    c1_3 = Consequence("Competitive market reaction", 1, impact_score=-5, probability=0.7)\n\n    # Second-order consequences\n    c2_1 = Consequence("Improved learning outcomes", 2, impact_score=9, probability=0.9)\n    c2_2 = Consequence("Higher user retention", 2, impact_score=7, probability=0.8)\n    c1_1.add_downstream(c2_1)\n    c1_1.add_downstream(c2_2)\n\n    c2_3 = Consequence("Need for additional funding", 2, impact_score=-6, probability=0.85)\n    c2_4 = Consequence("Delayed profitability", 2, impact_score=-5, probability=0.7)\n    c1_2.add_downstream(c2_3)\n    c1_2.add_downstream(c2_4)\n\n    c2_5 = Consequence("Price war initiation", 2, impact_score=-8, probability=0.6)\n    c2_6 = Consequence("Innovation race acceleration", 2, impact_score=4, probability=0.75)\n    c1_3.add_downstream(c2_5)\n    c1_3.add_downstream(c2_6)\n\n    # Third-order consequences\n    c3_1_positive = Consequence("Strong brand reputation", 3, impact_score=10, probability=0.9)\n    c3_1_negative = Consequence("Risk of market oversaturation", 3, impact_score=-6, probability=0.4)\n    c2_1.add_downstream(c3_1_positive)\n    c2_2.add_downstream(c3_1_positive)\n\n    c3_2 = Consequence("Reduced investor confidence", 3, impact_score=-9, probability=0.7)\n    c2_3.add_downstream(c3_2)\n\n    c3_3 = Consequence("Monopoly position achieved", 3, impact_score=10, probability=0.1)\n    c2_6.add_downstream(c3_3)\n    c2_6.add_downstream(c3_1_negative)\n\n    my_decision = Decision(\n        name="Launch AIEduPlatform",\n        description="Decision to launch a new AI-powered educational platform in Q3 2026."\n    )\n    my_decision.add_initial_consequence(c1_1)\n    my_decision.add_initial_consequence(c1_2)\n    my_decision.add_initial_consequence(c1_3)\n\n    # Convert to dict for the SaaS function\n    decision_payload = my_decision.to_dict()\n\n    print("--- Running Strategic Ripple Simulation (Max Order 3) ---")\n    simulation_results = run_simulation(decision_payload, max_order=3)\n    print(json.dumps(simulation_results, indent=2))