import json\n\nclass EffectGraph:\n    """\n    Represents the network of interconnected factors and their relationships.\n    Factors are nodes, relationships are directed edges with impact type and weight.\n    """\n    def __init__(self):\n        self.factors = {}\n\n    def add_factor(self, factor_name: str):\n        """Adds a new factor to the graph if it doesn't already exist."""\n        if factor_name not in self.factors:\n            self.factors[factor_name] = {}\n\n    def add_relationship(self, source_factor: str, target_factor: str,\n                         impact_type: str, weight: float):\n        """\n        Adds a directed relationship between two factors.\n        impact_type: "positive" or "negative"\n        weight: float between 0 and 1, representing the strength of the impact.\n        """\n        if source_factor not in self.factors:\n            self.add_factor(source_factor)\n        if target_factor not in self.factors:\n            self.add_factor(target_factor)\n\n        if impact_type not in ["positive", "negative"]:\n            raise ValueError("impact_type must be 'positive' or 'negative'")\n        if not (0 <= weight <= 1):\n            raise ValueError("weight must be between 0 and 1")\n\n        self.factors[source_factor][target_factor] = {"type": impact_type, "weight": weight}\n\n    def get_dependents(self, factor_name: str):\n        """Returns a dictionary of factors directly dependent on the given factor."""\n        return self.factors.get(factor_name, { })\n\n    def to_json(self):\n        """Exports the graph configuration to a JSON string."""\n        return json.dumps(self.factors, indent=2)\n\n    @classmethod\n    def from_json(cls, json_str: str):\n        """Loads a graph configuration from a JSON string."""\n        graph = cls()\n        graph.factors = json.loads(json_str)\n        return graph\n\nclass Action:\n    """\n    Represents an initial action or decision with its direct impacts on specific factors.\n    """\n    def __init__(self, name: str, direct_impacts: dict):\n        """\n        name: Name of the action.\n        direct_impacts: {factor_name: impact_value (float, e.g., 0.5 for positive, -0.5 for negative)}\n        """\n        self.name = name\n        self.direct_impacts = direct_impacts\n\nclass SecondOrderEffectSimulator:\n    """\n    Simulates the cascading consequences of an action through an effect graph.\n    """\n    def __init__(self, graph: EffectGraph):\n        self.graph = graph\n\n    def simulate(self, action: Action, max_depth: int = 3):\n        """\n        Runs the simulation to find direct and indirect effects.\n        Returns a dictionary of {factor_name: {"total_impact": float, "impact_details": [{"order": int, "value": float}]}}\n        """\n        simulation_results = {factor: {"total_impact": 0.0, "impact_details": []} for factor in self.graph.factors}\n        \n        q = []\n\n        for factor, impact_value in action.direct_impacts.items():\n            if factor in simulation_results: # Ensure factor exists in graph\n                simulation_results[factor]["total_impact"] += impact_value\n                simulation_results[factor]["impact_details"].append({"order": 1, "value": impact_value})\n                q.append((factor, impact_value, 1))\n\n        while q:\n            current_factor, current_impact_value, current_depth = q.pop(0)\n\n            if current_depth >= max_depth:\n                continue # Stop at max depth\n\n            dependents = self.graph.get_dependents(current_factor)\n            for dependent_factor, relationship in dependents.items():\n                if dependent_factor not in simulation_results:\n                    # Factor might be defined in a relationship but not as a top-level factor initially.\n                    # Add it to results if it's impacted.\n                    simulation_results[dependent_factor] = {"total_impact": 0.0, "impact_details": []}\n\n                relationship_impact = relationship["weight"]\n                if relationship["type"] == "negative":\n                    relationship_impact *= -1\n\n                # Calculate the propagated impact\n                # This is a simplified linear model. More complex models could be used.\n                propagated_impact = current_impact_value * relationship_impact \n\n                # Apply this to the dependent factor\n                simulation_results[dependent_factor]["total_impact"] += propagated_impact\n                simulation_results[dependent_factor]["impact_details"].append({"order": current_depth + 1, "value": propagated_impact})\n                \n                # Add to queue for further propagation\n                q.append((dependent_factor, propagated_impact, current_depth + 1))\n        \n        # Clean up results to only include factors that actually had an impact\n        final_results = {\n            factor: data for factor, data in simulation_results.items() \n            if data["total_impact"] != 0 or data["impact_details"]\n        }\n        \n        return final_results\n\n# --- Example Usage ---\nif __name__ == "__main__":\n    # 1. Define the Effect Graph\n    effect_graph = EffectGraph()\n    effect_graph.add_factor("Customer Satisfaction")\n    effect_graph.add_factor("Employee Morale")\n    effect_graph.add_factor("Product Quality")\n    effect_graph.add_factor("Market Share")\n    effect_graph.add_factor("Revenue")\n    effect_graph.add_factor("Brand Reputation")\n    effect_graph.add_factor("R&D Investment")\n    effect_graph.add_factor("Operational Costs")\n    effect_graph.add_factor("Regulatory Scrutiny")\n\n    effect_graph.add_relationship("Product Quality", "Customer Satisfaction", "positive", 0.8)\n    effect_graph.add_relationship("Customer Satisfaction", "Brand Reputation", "positive", 0.7)\n    effect_graph.add_relationship("Customer Satisfaction", "Market Share", "positive", 0.6)\n    effect_graph.add_relationship("Market Share", "Revenue", "positive", 0.9)\n    effect_graph.add_relationship("Brand Reputation", "Revenue", "positive", 0.5)\n    \n    effect_graph.add_relationship("Employee Morale", "Product Quality", "positive", 0.6)\n    effect_graph.add_relationship("Employee Morale", "Operational Costs", "negative", 0.4) # High morale -> lower costs\n    \n    effect_graph.add_relationship("R&D Investment", "Product Quality", "positive", 0.7)\n    \n    effect_graph.add_relationship("Operational Costs", "Revenue", "negative", 0.3) # High costs -> lower net revenue\n    effect_graph.add_relationship("Operational Costs", "Employee Morale", "negative", 0.2) # Cutting costs might impact morale negatively\n\n    # Example of a more complex chain\n    effect_graph.add_relationship("Regulatory Scrutiny", "Brand Reputation", "negative", 0.8)\n    effect_graph.add_relationship("Brand Reputation", "R&D Investment", "negative", 0.3) # Poor reputation might make investors hesitant for R&D\n\n    print("--- Effect Graph Definition (JSON) ---")\n    print(effect_graph.to_json())\n    print("\n" + "="*40 + "\n")\n\n    # 2. Define an Action\n    # Action: Implement a major cost-cutting measure\n    # This directly impacts:\n    #   - Operational Costs (positive change, i.e., costs go down, so negative impact on costs itself)\n    #   - Employee Morale (negative impact)\n    action_cost_cut = Action(\n        name="Implement Cost-Cutting Measure",\n        direct_impacts={\n            "Operational Costs": -0.4,  # e.g., 40% reduction in a relevant operational aspect\n            "Employee Morale": -0.3   # e.g., 30% drop due to layoffs/resource cuts\n        }\n    )\n\n    # 3. Initialize and Run the Simulator\n    simulator = SecondOrderEffectSimulator(effect_graph)\n    print(f"--- Simulating: '{action_cost_cut.name}' (Max Depth: 3) ---")\n    results_cost_cut = simulator.simulate(action_cost_cut, max_depth=3)\n\n    # 4. Print Results\n    for factor, data in results_cost_cut.items():\n        print(f"\nFactor: {factor}")\n        print(f"  Total Impact: {data['total_impact']:.2f}")\n        for detail in data['impact_details']:\n            print(f"    Order {detail['order']} Impact: {detail['value']:.2f}")\n\n    print("\n" + "="*40 + "\n")\n\n    # Another Action: Invest heavily in R&D\n    action_rd_invest = Action(\n        name="Heavy R&D Investment",\n        direct_impacts={\n            "R&D Investment": 0.5,\n            "Operational Costs": 0.1 # R&D investment might initially increase operational costs\n        }\n    )\n    print(f"--- Simulating: '{action_rd_invest.name}' (Max Depth: 3) ---")\n    results_rd_invest = simulator.simulate(action_rd_invest, max_depth=3)\n\n    for factor, data in results_rd_invest.items():\n        print(f"\nFactor: {factor}")\n        print(f"  Total Impact: {data['total_impact']:.2f}")\n        for detail in data['impact_details']:\n            print(f"    Order {detail['order']} Impact: {detail['value']:.2f}")\n